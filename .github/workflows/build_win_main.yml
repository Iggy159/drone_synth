name: Build and Test VST3 Plugin on Windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repo with submodules
        uses: actions/checkout@v3
        with:
          submodules: true

      - name: Set up MSVC developer environment
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure CMake (Release)
        run: cmake -B build -S ./drone -DCMAKE_BUILD_TYPE=Release -A x64

      - name: Build plugin
        run: cmake --build build --config Release

      - name: Show built files
        run: dir build\**\*.vst3

      - name: Upload VST3 (artifact)
        uses: actions/upload-artifact@v2
        with:
          name: DroneSynth-VST3
          path: build/**/*.vst3

  test-windows:
    needs: build-windows
    runs-on: windows-latest

    steps:
      - name: Download VST3 (artifact)
        uses: actions/download-artifact@v2
        with:
          name: DroneSynth-VST3
          path: downloaded-vst3

      - name: Show downloaded files
        run: dir downloaded-vst3\**\*.vst3

      # Здесь можешь добавить шаги тестирования или что угодно с файлами плагина
