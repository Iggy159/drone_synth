name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Hello world
        run: echo "✅ GitHub Actions работает!"
name: Build VST3 for Windows

on:
  workflow_dispatch:  # Это позволяет вручную запустить его

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Test step
        run: echo "✅ Workflow is working!"
